type TRoutes = {
  ROOT: string;
  REGISTER: string;
  LOGIN: string;
  TRANSACTIONS: string;
  HISTORY: string;
  TABLE: string;
  SETTINGS: string;
  REPORT: string;
};

export const RoutesName: TRoutes = {
  ROOT: '/',
  REGISTER: '/register',
  LOGIN: '/login',
  TRANSACTIONS: '/transactions',
  HISTORY: '/history',
  TABLE: '/table',
  SETTINGS: '/settings',
  REPORT: '/report',
};
